spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:postgresql://localhost:5433/jira-test
    username: ${CI_login_DB_test:jira}
    password: ${CI_password_DB_test:JiraRush}